import {StyleSheet } from "react-native";


const styles = StyleSheet.create({
    boxTitle: {
        alignItems: 'center',
        marginTop: 50,
        padding: 15,
    },
    titleText: {
        color: "#e6c700",
        fontWeight: 'bold',
        fontSize: 30,
    }
});

export default styles;